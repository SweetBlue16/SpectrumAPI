spring:
  config:
    import: optional:file:../../infra/.env[.properties]
  data: 
    mongodb:
      uri: mongodb://${DB_MONGO_USER:spectrum_admin}:${DB_MONGO_PASSWORD:SpectrumPassword050911!}@${DB_MONGO_HOST:localhost}:${DB_MONGO_PORT:27017}/${DB_MONGO_SOCIAL_NAME:spectrum_social}?authSource=admin

grpc:
  server:
    port: ${GRPC_SERVER_PORT:9091}